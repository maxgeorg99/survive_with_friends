// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { AgnaPhase2FlamethrowerScheduler } from "./agna_phase_2_flamethrower_scheduler_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `agna_phase2_flamethrower_scheduler`.
 *
 * Obtain a handle from the [`agnaPhase2FlamethrowerScheduler`] property on [`RemoteTables`],
 * like `ctx.db.agnaPhase2FlamethrowerScheduler`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.agnaPhase2FlamethrowerScheduler.on_insert(...)`.
 */
export class AgnaPhase2FlamethrowerSchedulerTableHandle {
  tableCache: TableCache<AgnaPhase2FlamethrowerScheduler>;

  constructor(tableCache: TableCache<AgnaPhase2FlamethrowerScheduler>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<AgnaPhase2FlamethrowerScheduler> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `scheduledId` unique index on the table `agna_phase2_flamethrower_scheduler`,
   * which allows point queries on the field of the same name
   * via the [`AgnaPhase2FlamethrowerSchedulerScheduledIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.agnaPhase2FlamethrowerScheduler.scheduledId().find(...)`.
   *
   * Get a handle on the `scheduledId` unique index on the table `agna_phase2_flamethrower_scheduler`.
   */
  scheduledId = {
    // Find the subscribed row whose `scheduledId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): AgnaPhase2FlamethrowerScheduler | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.scheduledId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: AgnaPhase2FlamethrowerScheduler) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: AgnaPhase2FlamethrowerScheduler) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: AgnaPhase2FlamethrowerScheduler) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: AgnaPhase2FlamethrowerScheduler) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: AgnaPhase2FlamethrowerScheduler, newRow: AgnaPhase2FlamethrowerScheduler) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: AgnaPhase2FlamethrowerScheduler, newRow: AgnaPhase2FlamethrowerScheduler) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
