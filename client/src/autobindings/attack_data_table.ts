// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
import {
  AlgebraicType as __AlgebraicTypeValue,
  BinaryReader as __BinaryReader,
  BinaryWriter as __BinaryWriter,
  ClientCache as __ClientCache,
  ConnectionId as __ConnectionId,
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  Identity as __Identity,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TableCache as __TableCache,
  TimeDuration as __TimeDuration,
  Timestamp as __Timestamp,
  deepEqual as __deepEqual,
  type AlgebraicType as __AlgebraicTypeType,
  type AlgebraicTypeVariants as __AlgebraicTypeVariants,
  type CallReducerFlags as __CallReducerFlags,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
} from "spacetimedb";
import { AttackData } from "./attack_data_type";
import { AttackType } from "./attack_type_type";
// Mark import as potentially unused
declare type __keep_AttackType = AttackType;

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";
declare type __keep = [EventContext, Reducer, RemoteReducers, RemoteTables];

/**
 * Table handle for the table `attack_data`.
 *
 * Obtain a handle from the [`attackData`] property on [`RemoteTables`],
 * like `ctx.db.attackData`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.attackData.on_insert(...)`.
 */
export class AttackDataTableHandle {
  tableCache: __TableCache<AttackData>;

  constructor(tableCache: __TableCache<AttackData>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<AttackData> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `attackId` unique index on the table `attack_data`,
   * which allows point queries on the field of the same name
   * via the [`AttackDataAttackIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.attackData.attackId().find(...)`.
   *
   * Get a handle on the `attackId` unique index on the table `attack_data`.
   */
  attackId = {
    // Find the subscribed row whose `attackId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): AttackData | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.attackId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `attackType` unique index on the table `attack_data`,
   * which allows point queries on the field of the same name
   * via the [`AttackDataAttackTypeUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.attackData.attackType().find(...)`.
   *
   * Get a handle on the `attackType` unique index on the table `attack_data`.
   */
  attackType = {
    // Find the subscribed row whose `attackType` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: AttackType): AttackData | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.attackType, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: AttackData) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: AttackData) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: AttackData) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: AttackData) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: AttackData, newRow: AttackData) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: AttackData, newRow: AttackData) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
