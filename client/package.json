{
  "name": "vibe_survivors_client",
  "homepage": "https://www.survive-with-friends.com/",
  "version": "1.0.0",
  "description": "Phaser client for Vibe Survivors",
  "main": "src/main.ts",
  "scripts": {
    "start": "vite --host",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy": "npm run build && echo survive-with-friends.com > dist/CNAME",
    "deploy": "gh-pages -d dist",
    "generate-bindings": "spacetimedb generate --lang typescript --out-dir ./src/autobindings"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.80.1",
    "@clockworklabs/spacetimedb-sdk": "latest"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.11"
  }
}