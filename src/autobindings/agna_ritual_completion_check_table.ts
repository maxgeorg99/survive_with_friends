// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { AgnaRitualCompletionCheck } from "./agna_ritual_completion_check_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `agna_ritual_completion_check`.
 *
 * Obtain a handle from the [`agnaRitualCompletionCheck`] property on [`RemoteTables`],
 * like `ctx.db.agnaRitualCompletionCheck`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.agnaRitualCompletionCheck.on_insert(...)`.
 */
export class AgnaRitualCompletionCheckTableHandle {
  tableCache: TableCache<AgnaRitualCompletionCheck>;

  constructor(tableCache: TableCache<AgnaRitualCompletionCheck>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<AgnaRitualCompletionCheck> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `scheduledId` unique index on the table `agna_ritual_completion_check`,
   * which allows point queries on the field of the same name
   * via the [`AgnaRitualCompletionCheckScheduledIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.agnaRitualCompletionCheck.scheduledId().find(...)`.
   *
   * Get a handle on the `scheduledId` unique index on the table `agna_ritual_completion_check`.
   */
  scheduledId = {
    // Find the subscribed row whose `scheduledId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): AgnaRitualCompletionCheck | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.scheduledId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: AgnaRitualCompletionCheck) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: AgnaRitualCompletionCheck) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: AgnaRitualCompletionCheck) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: AgnaRitualCompletionCheck) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: AgnaRitualCompletionCheck, newRow: AgnaRitualCompletionCheck) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: AgnaRitualCompletionCheck, newRow: AgnaRitualCompletionCheck) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
