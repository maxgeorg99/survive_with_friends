// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { HealthRegenScheduler } from "./health_regen_scheduler_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `health_regen_scheduler`.
 *
 * Obtain a handle from the [`healthRegenScheduler`] property on [`RemoteTables`],
 * like `ctx.db.healthRegenScheduler`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.healthRegenScheduler.on_insert(...)`.
 */
export class HealthRegenSchedulerTableHandle {
  tableCache: TableCache<HealthRegenScheduler>;

  constructor(tableCache: TableCache<HealthRegenScheduler>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<HealthRegenScheduler> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `scheduledId` unique index on the table `health_regen_scheduler`,
   * which allows point queries on the field of the same name
   * via the [`HealthRegenSchedulerScheduledIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.healthRegenScheduler.scheduledId().find(...)`.
   *
   * Get a handle on the `scheduledId` unique index on the table `health_regen_scheduler`.
   */
  scheduledId = {
    // Find the subscribed row whose `scheduledId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): HealthRegenScheduler | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.scheduledId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: HealthRegenScheduler) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: HealthRegenScheduler) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: HealthRegenScheduler) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: HealthRegenScheduler) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: HealthRegenScheduler, newRow: HealthRegenScheduler) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: HealthRegenScheduler, newRow: HealthRegenScheduler) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
