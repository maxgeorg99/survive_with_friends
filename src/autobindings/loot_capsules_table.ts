// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { LootCapsules } from "./loot_capsules_type";
import { DbVector2 as __DbVector2 } from "./db_vector_2_type";
import { GemLevel as __GemLevel } from "./gem_level_type";

import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `loot_capsules`.
 *
 * Obtain a handle from the [`lootCapsules`] property on [`RemoteTables`],
 * like `ctx.db.lootCapsules`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.lootCapsules.on_insert(...)`.
 */
export class LootCapsulesTableHandle {
  tableCache: TableCache<LootCapsules>;

  constructor(tableCache: TableCache<LootCapsules>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<LootCapsules> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `capsuleId` unique index on the table `loot_capsules`,
   * which allows point queries on the field of the same name
   * via the [`LootCapsulesCapsuleIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.lootCapsules.capsuleId().find(...)`.
   *
   * Get a handle on the `capsuleId` unique index on the table `loot_capsules`.
   */
  capsuleId = {
    // Find the subscribed row whose `capsuleId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): LootCapsules | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.capsuleId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: LootCapsules) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: LootCapsules) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: LootCapsules) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: LootCapsules) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: LootCapsules, newRow: LootCapsules) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: LootCapsules, newRow: LootCapsules) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
