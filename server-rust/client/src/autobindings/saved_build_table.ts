// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { SavedBuild } from "./saved_build_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `saved_build`.
 *
 * Obtain a handle from the [`savedBuild`] property on [`RemoteTables`],
 * like `ctx.db.savedBuild`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.savedBuild.on_insert(...)`.
 */
export class SavedBuildTableHandle {
  tableCache: TableCache<SavedBuild>;

  constructor(tableCache: TableCache<SavedBuild>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<SavedBuild> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `buildId` unique index on the table `saved_build`,
   * which allows point queries on the field of the same name
   * via the [`SavedBuildBuildIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.savedBuild.buildId().find(...)`.
   *
   * Get a handle on the `buildId` unique index on the table `saved_build`.
   */
  buildId = {
    // Find the subscribed row whose `buildId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): SavedBuild | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.buildId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: SavedBuild) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: SavedBuild) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: SavedBuild) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: SavedBuild) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: SavedBuild, newRow: SavedBuild) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: SavedBuild, newRow: SavedBuild) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
